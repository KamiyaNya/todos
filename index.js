const express = require('express')
const mongoose = require('mongoose')
const exphbs = require('express-handlebars')

const start = async () => {
    try {

    } catch (e) {
        console.log(e)
    }
}
start()